angular.module('UserService', [])
	.factory('UserAPIService', function($http) {

		UserAPIService = {
			callAPI: function(url, data)	{
				registrationUser: function(url, data);
					}
			}
			return UserAPIService;
	});